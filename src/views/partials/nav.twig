
<nav class="navbar navbar-expand-sm bg-light">

    <!-- Links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a href="{{ path_for('home') }}" class="nav-link">Home</a>
        </li>
        <li class="nav-item">
            <a href="{{ path_for('about') }}" class="nav-link">About us</a>
        </li>
        <li class="nav-item">
            <a href="{{ path_for('cart') }}" class="nav-link">Cart</a>
        </li>

        {% if User.loggedIn %}
            <li class="nav-item">
                <a href="{{ path_for('logout') }}" class="nav-link">Logout</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a href="{{ path_for('login') }}" class="nav-link">Login</a>
            </li>
            <li class="nav-item">
                <a href="{{ path_for('register') }}" class="nav-link">Register</a>
            </li>
        {% endif %}
    </ul>

</nav>